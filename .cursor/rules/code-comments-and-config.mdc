---
description: 配置项与关键代码必须附中文说明和示例，便于初学者阅读
alwaysApply: true
---

# 配置与代码注释规范

**目标**：初学者与后续维护者能**不查外部文档**也能从文件本身理解「是什么、为什么、怎么用」。

## 一、配置文件（必须）

适用：`config/app.yaml`、`config/*.yaml`、`.env.example`、以及未来 `config.override.yaml`。

每个**配置项**（或逻辑分组）须包含：

| 要素 | 说明 |
|------|------|
| **中文含义** | 这一项控制什么行为 |
| **可选值 / 类型** | 合法取值或单位 |
| **示例** | 至少一个 `# 示例: ...` 或注释内嵌示例 |
| **与谁相关** | 若与 `.env` 或其它配置重复，写清分工 |

文件顶部须有**总览注释**：本文件管什么、谁改、改完如何生效、权威文档章节（如 `sec-08`）。

## 二、Python 代码（必须）

### 2.1 模块级（文件顶部）

除模块 docstring 外，关键模块须说明：

- **职责**（这一文件在整条链路里干什么）
- **何时被调用**（谁 import、哪一阶段）
- **简例**（输入 → 输出，一行伪代码即可）

### 2.2 函数 / 类 / 变量

- 函数、类、公共变量：**用途、参数、返回值**（已有规范，保持）
- **不够时须补充**：
  - 关键分支**为什么**这样写
  - 与架构概念对应（如 L1/L2/L3、state.skill vs state.prompt）
  - 非显而易见的步骤用**行内中文注释**（ `# 步骤 2：...` ）

### 2.3 关键路径

下列路径视为**关键代码**，注释密度高于普通工具函数：

- `app/core/config*.py`、`profile_presets.py`、`runtime_config.py`
- `app/skills/`（Registry 扫描与加载）
- `app/graph/`、`app/services/chat*.py`（实现后）
- `app/api/chat.py`（实现后）

**禁止**：只有函数 docstring、模块内无任何流程说明的「裸代码」。

## 三、新增配置时的同步

1. 在 `config/app.yaml`（或 `.env.example`）写清中文说明 + 示例
2. 在 `docs/ARCHITECTURE.html` 对应 sec 补一行或更新表格（一处权威）
3. 在 `docs/DEVELOPMENT_LOG.html` 变更记录追加一步（若为本阶段新工作）
4. 代码中引用新键名时，字面量放入 `config_constants.py` 或 `constants.py`，并注释含义

## 三之二、字面量治理（约定大于配置的前提）

**核心**：约定必须有唯一权威定义处。同一字面量多处重复定义或内联在逻辑里，即违规。
归属决策表与常量模块索引见 `docs/ARCHITECTURE.html` sec-08 8.5（一处权威，此处只列规则）。

| 规则 | 说明 |
|---|---|
| **一处权威** | 跨层契约（角色、SSE 事件/字段、domain 取值）只在 `app/core/constants.py` 定义；各层需要别名时用 `X = core_constants.Y` 引用，不复制字符串 |
| **层内集中** | 某层独用的协议键/名称放该层 `*_constants.py`（providers、storage、retrieval、graph 均已有） |
| **写读同源** | 一方产出、另一方消费的键名（Cypher RETURN 别名、prompt 段落标题）双方 import 同一常量；测试断言也引用常量而非复制字面量 |
| **约定可覆盖** | 业务默认值做成「常量默认 + app.yaml 可覆盖」（如 route.keywords），而非只能改代码 |
| **例外** | 测试基建（`sqlite:///:memory:`）、docstring 示例、日志文案可内联 |

## 四、自检清单

```text
□ 配置文件每个键都有中文说明和示例
□ 关键模块 docstring 含「职责 + 何时调用 + 简例」
□ 核心流程函数内有步骤级中文注释
□ 初学者能回答：这项配置改了会影响什么行为
```
