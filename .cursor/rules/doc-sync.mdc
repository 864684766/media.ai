---
description: 写代码前先检索并完善相关文档；写代码后同步更新，保持文档与代码一致
alwaysApply: true
---

# 文档同步规则

**核心原则**：文档与代码同步演进。先文档、后代码；代码变更后，文档必须跟上。

## 一、写代码之前（必须先做）

在新增、修改任何代码/配置/接口之前，**必须先检索并完善相关文档**，不得直接动手写代码。

### 1.1 检索方式

按以下顺序定位文档，**至少完成前两步**：

1. **按文档文件名检索**（Glob / 目录浏览）
2. **按文档简介/章节标题检索**（Grep 关键词，见下方「文档索引」）
3. **按代码目录反查**（见「模块 → 文档映射表」）

### 1.2 写代码前的文档动作

| 情况 | 动作 |
|------|------|
| 已有相关文档 | **先阅读** → 对照当前设计 → **先更新文档**（补缺失、改过时、标清「已实现/待实现」）→ 再写代码 |
| 无相关文档 | **先新建或补充**对应章节/说明 → 再写代码 |
| 文档与计划不一致 | **先统一文档**（以架构为准或确认变更）→ 再写代码 |

### 1.3 写代码前检查清单

```text
□ 已用文件名 + 关键词检索到相关文档
□ 已阅读映射表中列出的权威章节
□ 文档已反映本次要做的范围（字段、路径、示例、状态标注）
□ 若涉及配置/环境变量，.env.example 说明已预备或已更新
□ 确认可以开始写代码
```

## 二、写代码之后（必须做）

每次完成代码开发或功能变更后，必须执行文档同步检查，确保文档与代码一致。

### 2.1 检查时机

- 新增、修改、删除代码、配置、接口、目录结构后
- 提交或结束开发任务前
- `poetry run pytest` 通过后

### 2.2 检查范围

按变更影响定位相关文档（见映射表），常见位置：

- `docs/index.html`：项目入门（安装、运行、API 列表）
- `docs/DEVELOPMENT_LOG.html`：开发进度与术语速查、视频 Phase V 待办
- `docs/ARCHITECTURE.html`：架构设计主文档（含 Vue Flow 流程图、配置/API 契约）
- `docs/VIDEO_PIPELINE.html`：视频生产线权威设计（分镜/bible/切片/QA/合成）
- `docs/js/`：架构流程图脚本（`flow-diagram-*.js`）
- `.env.example`：环境变量与配置项说明
- `config/app.yaml`：运维可编辑配置（含注释）
- `.cursor/plans/`：阶段计划与架构决策（如有）

### 2.3 四项操作

| 操作 | 说明 |
|------|------|
| **删除** | 移除已废弃功能、已删除文件/接口对应的文档描述 |
| **更新** | 修正与当前代码不一致的路径、命令、参数、流程、示例 JSON |
| **添加** | 为新功能、新接口、新配置项补充缺失说明 |
| **避免冗余** | 不重复描述；**一处权威、其余引用** |

### 2.4 执行顺序

```text
1. 识别本次变更影响的模块
2. 按「文档索引」和「映射表」定位相关文档
3. 逐项对照代码：删无用、改过时、补缺失、标注实现状态
4. 若改了流程/架构，同步 docs/js/ 中对应 Vue Flow 图
5. 运行 poetry run pytest 验证
6. 确认文档与代码一致后再结束任务
```

## 三、文档索引（按文件名与简介检索）

| 文档路径 | 简介 | 检索关键词示例 |
|----------|------|----------------|
| `docs/index.html` | 项目入门：安装、运行、目录、API 列表、文档入口 | readme、运行、安装、api |
| `docs/ARCHITECTURE.html` | **主文档**：架构、流程图、配置、存储、API 契约、路线图 | architecture、架构、sec- |
| `docs/VIDEO_PIPELINE.html` | **视频生产线**：结构化分镜、bible、切片/QA/合成、Phase V 待办 | video、分镜、shot、合成、一致性、Phase V |
| `docs/DEVELOPMENT_LOG.html` | **开发进度日志**：按步骤摘要、术语速查、当前状态、视频待办 | development、进度、步骤、术语、sec-video-todos |
| `.env.example` | 环境变量模板与说明 | env、环境变量、APP_、DATABASE |
| `config/app.yaml` | 运维 Profile 与业务配置（实现后） | app.yaml、profile、配置 |
| `.cursor/rules/doc-sync.mdc` | 本文档同步规则 | doc-sync、文档同步 |
| `.cursor/rules/frontend-shadcn-ui.mdc` | media-web 前端 UI：一律 shadcn/ui | shadcn、Button、Dialog、禁止原生 |
| `docs/js/flow-diagram-*.js` | Vue Flow 交互流程图数据 | flow-diagram、vue flow、流程图 |

### ARCHITECTURE.html 章节索引

| 章节 ID | 标题 | 适用模块 |
|---------|------|----------|
| `sec-03` | 总体架构 | 全流程、请求链路 |
| `sec-04` | 存储架构 | PostgreSQL、Neo4j、同步 |
| `sec-05` | Provider Registry | LLM 厂商、模型 |
| `sec-06` | RAG 与关键词检索 | 检索基础 |
| `sec-07` | 检索准确性工程 | Hybrid、Grader、Web 兜底 |
| `sec-08` | **配置体系** | ConfigResolver、Profile、app.yaml |
| `sec-09` | LangGraph 流程 | 状态机、节点 |
| `sec-10` | Agent Skills | skills/、Skill Registry |
| `sec-10-4` | 视频 Skill 文本层 | director、storyboard、creation_type |
| `sec-11` | MCP | 外部工具、视频 render/compose 契约引用 |
| `sec-13` | Ingestion Lite | 数据导入、切分 |
| `sec-14` | 多轮对话与历史 | retention、compact |
| `sec-15` | **API 与数据格式** | ChatRequest、AgentState、SSE |
| `sec-16` | 路线图与评测 | Phase 进度、Phase V |
| `sec-16-phase-v` | Phase V 视频生产线 | 分镜、切片、合成 |
| `appendix` | 目标目录结构 | 新增目录/文件时对照 |

### VIDEO_PIPELINE.html 章节索引（视频权威）

| 章节 ID | 标题 | 适用模块 |
|---------|------|----------|
| `sec-status` | 现状与边界 | 文本层 vs 生产线 |
| `sec-flow` | 目标端到端流 | 状态机 |
| `sec-data` | 数据模型 | shots、bible、compose_jobs |
| `sec-graph` | LangGraph 视频子图 | app/graph、app/video |
| `sec-continuity` | 一致性严控 | QA、长视频 |
| `sec-mcp` | MCP / 服务契约 | render_shot、compose |
| `sec-api` | API 草案 | /api/v1/video |
| `sec-phases` | 分阶段验收 | V0–V7 |
| `sec-todos` | 开发待办 | 排期 |

## 四、模块 → 文档映射表

开发某模块时，**写代码前**须检索下表「权威文档」；**写代码后**须同步「同步文档」全部相关项。

| 代码模块/目录 | 权威文档（详细说明） | 同步文档（写代码后必查） |
|---------------|----------------------|--------------------------|
| `app/core/config*.py`、`config/app.yaml` | ARCHITECTURE `sec-08` | `.env.example`、`docs/index.html`、ARCHITECTURE `sec-08` |
| `app/providers/` | ARCHITECTURE `sec-05` | `.env.example`、`sec-08` |
| `app/storage/postgres/` | ARCHITECTURE `sec-04` | `.env.example`、`appendix` |
| `app/storage/neo4j/` | ARCHITECTURE `sec-04` | `.env.example`、`appendix` |
| `app/retrieval/` | ARCHITECTURE `sec-06`、`sec-07` | `flow-diagram-retrieval.js` |
| `app/graph/` | ARCHITECTURE `sec-09` | `flow-diagram-langgraph.js`、`sec-15` |
| `app/skills/`、`skills/` | ARCHITECTURE `sec-10` | `docs/index.html`、`DEVELOPMENT_LOG.html` |
| `app/video/`、视频 Job/分镜 | `docs/VIDEO_PIPELINE.html` | ARCHITECTURE `sec-10.4`、`sec-16`、`DEVELOPMENT_LOG.html#sec-video-todos` |
| `app/mcp/` | ARCHITECTURE `sec-11`；视频工具另见 `VIDEO_PIPELINE.html#sec-mcp` | `flow-diagram-mcp.js` |
| `app/ingestion/` | ARCHITECTURE `sec-13` | `flow-diagram-ingestion.js`、`sec-04` |
| `app/services/` | ARCHITECTURE `sec-03`、`sec-15` | `docs/index.html` |
| `app/schemas/` | ARCHITECTURE `sec-15` | `docs/index.html` |
| `app/api/chat.py` | ARCHITECTURE `sec-15` | `docs/index.html` API 表、`flow-diagram-request.js` |
| `app/api/` 新路由 | ARCHITECTURE `sec-15` | `docs/index.html` API 表 |
| 项目结构变更 | `appendix` | `docs/index.html` 项目结构 |

## 五、禁止事项

- **禁止**未检索、未完善相关文档就直接写代码
- 不保留已删除接口或文件的历史描述
- 不复制粘贴大段重复内容到多个文档
- 不在文档中把尚未实现的功能写成「已有功能」（须标注「设计稿 / Phase N 待实现 / 已实现」）
- **禁止**新增项目文档 `.md`（见 `docs-html-only.mdc`）；文档一律 `docs/*.html`

## 六、配置与代码注释（与 doc-sync 同步）

详见 [`.cursor/rules/code-comments-and-config.mdc`](code-comments-and-config.mdc)：

- **每个配置项**：中文含义 + 可选值 + 示例（`config/app.yaml`、`.env.example`）
- **关键代码**：模块级说明流程与调用时机；核心函数内步骤级中文注释，不能只有函数 docstring
