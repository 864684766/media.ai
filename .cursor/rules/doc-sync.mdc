---
description: 每次开发后同步检查并更新相关文档，保持文档与代码一致
alwaysApply: true
---

# 文档同步规则

每次完成代码开发或功能变更后，必须执行文档同步检查，确保文档与代码一致。

## 检查时机

- 新增、修改、删除代码、配置、接口、目录结构后
- 提交或结束开发任务前
- build / pytest 通过后

## 检查范围

按变更影响定位相关文档，常见位置：

- `README.md`：项目说明、运行方式、目录结构、API 列表
- `docs/ARCHITECTURE.html`：架构设计文档（主文档，含 Vue Flow 交互流程图）
- `docs/js/`：架构图脚本与辅助资源
- `.env.example`：环境变量与配置项
- 计划文档（`.cursor/plans/`）：架构决策与阶段路线

## 四项操作

| 操作 | 说明 |
|------|------|
| **删除** | 移除已废弃功能、已删除文件/接口对应的文档描述 |
| **更新** | 修正与当前代码不一致的路径、命令、参数、流程 |
| **添加** | 为新功能、新接口、新配置补充缺失说明 |
| **避免冗余** | 不重复描述已在其他文档说明的内容；一处权威、其余引用 |

## 执行顺序

```text
1. 识别本次变更影响的模块
2. 定位相关文档文件
3. 逐项对照代码：删无用、改过时、补缺失
4. 运行 build / pytest 验证
5. 确认文档与代码一致后再结束任务
```

## 禁止事项

- 不保留已删除接口或文件的历史描述
- 不复制粘贴大段重复内容到多个文档
- 不在文档中描述尚未实现的“计划功能”为“已有功能”
